const listContainer = document.querySelector('.container');
const textInput = document.querySelector('.the-input');
const clearBtn = document.querySelector('.clear');

export { listContainer, textInput, clearBtn };